#include <stdio.h>

/**
 * main - check the code
 * @argc: Argument count.
 * @argv: Parameters.
 *
 * Return: Always 0.
 */
int main(int argc, char **argv)
{
	if (argc > 0)
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
